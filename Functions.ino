/*
 * All of the calcultaions will e done here
*/

/*
 * All of the calcultaions will e done here
*/

int getNextNote(char type,int keyNote ,int currentNoteIndex){
  int offsetIndex;
  int offset;
  return 0;
  }

  int getNoteForButton (int buttonNumber){
    switch(currentScaleType){
      case 1: return KEY_NOTE+MAJOR[buttonNumber-1];
      case 2: return KEY_NOTE+MINOR[buttonNumber-1];
      case 3: return KEY_NOTE+DIMINISHED[buttonNumber-1];
      }
  }
